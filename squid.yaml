manifestVersion: subsquid.io/v0.1
name: zenlink-moonbeam-squid
version: 4
description: |-
  Zenlink Moonbeam Squid
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "npx", "squid-graphql-server"]
